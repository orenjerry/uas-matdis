# Tugas Matematika Diskrit

Jalankan perintah ini untuk menginstall depedency yang dibutuhkan
```ssh
pip install pygame
```
Atau
```ssh
pip install -r requirements.txt
```

Setelah menginstall jalankan programnya dengan
```ssh
python Matdis.py
```